#!/bin/sh
# Start X server with key repeating starting when you hold a key for
# at least 170ms (or 0.17s), and repeat the key every 16ms (or 0.016s)
# And some other thing to don't allow connections from outside I guess lol
exec /usr/bin/X -ardelay 170 -arinterval 16 -nolisten tcp "$@"
